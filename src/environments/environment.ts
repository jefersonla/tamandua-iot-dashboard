// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
        apiKey: "AIzaSyANTPkWd9P1nYQqBk8q46gr0ej4CfsbXIE",
        authDomain: "teiu-dev.firebaseapp.com",
        databaseURL: "https://teiu-dev.firebaseio.com",
        projectId: "teiu-dev",
        storageBucket: "teiu-dev.appspot.com",
        messagingSenderId: "479254838310",
        appId: "1:479254838310:web:4a9b2f12ad8a01e3"
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
